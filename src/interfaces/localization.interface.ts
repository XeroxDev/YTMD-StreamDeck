export interface LocalizationInterface {
    PI: {
        HOST: string;
        PORT: string;
        PASSWORD: string;
        VOLUME_STEPS: string;
        SAVE: string;
        AUTOMATIC_SAVE: string;
    };
}
